#include "main.h"
/**
 * print_last_digit - prints the last digit of a number
 * @c: the number whose last digit is taken
 * Return: the last digit of the number
 */
int print_last_digit(int c)
{
	int rem;

	rem = c % 10;
	return (rem);
}
