#include "main.h"
/**
 * _abs -  calculates the absolute value of a number
 * @c: the number used in the absolute value
 * Return: c or -c depending on the number
 */
int _abs(int c)
{
	if (c >= 0)
	{
		return (c);
	}
	else
	{
		return (-c);
	}
}


